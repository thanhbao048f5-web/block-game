# 🎮 下落方块音游 · 使用说明

> 一个像 B 站「音游，但你干嘛~」那样的下落方块游戏。
> **双击 `index.html` 就能玩**，不需要安装任何软件，手机、电脑都能玩。

---

## 一、怎么玩

1. **双击打开 `index.html`**
2. 点击粉红色按钮 **「🎮 开始游戏」**
3. 彩色方块会从屏幕顶部随机落下，**在方块落到底部的粉色判定线之前，点击它**（或用键盘按键）
4. 点中 → 方块碎裂消失 + 发出声音 + 得分
5. 漏掉方块 / 点空 → 扣 ❤️（3 条命，每得 200 分 +1 条，最多 5 条）
6. 命用完 → 游戏结束，看分数和连击

| 操作 | 方式 |
|---|---|
| 点方块 | 🖱 鼠标点击 / 👆 手机手指点按 |
| 键盘 | `A S D F`（3 列时 `A S D`，5 列时 `A S D F G`） |
| 暂停 | `P` 键或右上角 ⏸ 按钮 |
| 设置 | 右上角 ⚙️ 按钮 |

**难度会自动递增**：分数越高，方块下落越快、越密。也可以在设置里手动调整。

---

## 二、怎么定制声音（你的重点）

点右上角 **⚙️ 设置** → **「🎵 音效设置」**，有 **3 种方式**：

### 方式 1：内置合成音（最简单，不用任何文件）
选「内置合成」，再挑音色：
- 🎹 钢琴　⚡ 电子　🥁 鼓点　🪘 木琴
- 连击越高，音调越高（像弹琴一样）

### 方式 2：上传你自己的音频（mp3 / wav）
1. 选「**上传音频**」
2. 每个轨道点「📁 选择」，选一个声音文件（如「你干嘛~.mp3」）
3. 上传成功后显示 **✓ 文件名**，点「▶ 试听」可以马上听
4. 点击方块时，对应轨道就播放你上传的声音

### 方式 3：录音（录你自己的声音）
1. 选「**录音**」
2. 点「🎙 录音」→ 开始录 → 再点一次「⏹ 停止」
3. 录完自动保存，点击方块时播放

> ⚠️ **注意**：录音功能需要 **https 网页环境**（部署到网上后用），直接双击打开文件不支持录音——这是浏览器安全规则，上传音频文件则没有这个限制。

### 音量
「音量」滑块随时调，0~100%。

---

## 三、怎么定制方块图形（也是你的重点）

设置 → **「🎨 方块外观（图形可定制）」**，4 种类型：

| 类型 | 说明 |
|---|---|
| 🌈 渐变 | 每列不同颜色，默认样式，最好看 |
| 🎨 纯色 | 选一个喜欢的颜色（预设 6 色 + 自定义取色器） |
| 😀 Emoji | 方块里显示一个表情符号，输入框随便填（🎵 🐱 🔥 🐶 …） |
| 🖼 图片 | **上传你自己的图片当方块**（梗图、表情包、你的头像都行） |

**形状** 也能换：圆角方块 / 圆形 / 菱形 / 六边形。

> 💡 图片建议用**正方形**（如 500×500），效果最好。上传后底部的预览区会立刻显示效果。

---

## 四、怎么调难度

设置 → **「🎯 游戏难度」**：
- **轨道数**：3 / 4 / 5 列（4 列最标准，5 列最刺激）
- **下落速度**：1~10 滑块（无论多快，方块都保持整齐的多排网格）
- **严格模式**：打开后，点空也扣 ❤️（挑战模式）

**布局说明**：屏幕是**多排网格**——每一排只有 1 个方块（随机出现在某一列），整屏有很多排同时下落，像 B 站那个"音游"一样整齐。

**所有设置都会自动保存**，下次打开还是你的配置。

---

## 五、🎬 视频方块模式（把视频拆成方块！）

这就是把你的微信视频变成游戏的方法：视频按 **0.2 秒一段**切开，每段拆成「一张画面 + 一段声音」，画面当方块、声音当点击音效。连起来点，声音就是原视频的节奏！

### 素材文件夹（已经给你做好了）

```
下落方块音游/
├── index.html           ← 游戏
└── video_assets/        ← 视频素材（本机已含你的微信视频切片）
    ├── manifest.js      ← 素材清单
    ├── bg.mp4           ← 背景视频（画面+原声）
    ├── frames/          ← 135 张画面（f_0001.jpg ~ f_0135.jpg）
    └── sounds/          ← 135 段声音（s_0001.wav ~ s_0135.wav）
```

### 玩法（点击驱动：点一下，画面动一下，音乐响一下）

1. 打开游戏 → ⚙️ 设置 → **「🎬 视频方块模式」**（默认已开启）
2. 开始游戏 → 屏幕是**整齐的多排网格**：每排 1 个方块（随机列），背景视频静止不动
3. **点击方块** → 背景视频跳到那一帧**播放一小段（画面+音乐同步）**，方块碎裂得分
4. **点得快（约 15 秒点满 20 个）** → 背景视频连续滚动、音乐连成**一首完整的歌**！
5. 停止点击约 1.5 秒 → 背景自动停回静止

### 调整「一首歌分几段」

设置 → 视频方块模式 → **「点几下连成一首歌」**（默认 20 下）：
- 数值越大 → 每段越短，需要点更多下
- 数值越小 → 每段越长，几下就听完全曲

### 怎么自己做素材（换别的视频）

1. 准备一个 mp4 视频
2. 用 AI 帮忙跑下面两条命令（ffmpeg），或者直接让 AI 帮你切：

```
# 切声音（每 0.2s 一段 wav）
ffmpeg -i 视频.mp4 -f segment -segment_time 0.2 -ar 22050 -ac 1 -c:a pcm_s16le sounds/s_%04d.wav
# 切画面（每 0.2s 一帧 jpg）
ffmpeg -i 视频.mp4 -vf "fps=5,scale=160:160" -q:v 2 frames/f_%04d.jpg
# 压背景视频（保留原声）
ffmpeg -i 视频.mp4 -vf "scale=480:480" -c:v libx264 -crf 30 -movflags +faststart -c:a aac -b:a 96k bg.mp4
```

3. 把切好的文件放进 `video_assets/`，再更新 `manifest.js` 里的片段数量即可
4. 发送给朋友时，**把整个文件夹一起发**（游戏 + video_assets）

> 💡 没有 video_assets 素材时，视频模式自动无效，游戏照常玩普通模式。

---

## 六、怎么分享给别人

### 方式 1：直接发文件（最简单）
把 `index.html` 用微信/QQ 发给朋友，对方**双击就能玩**，不需要任何软件。

### 方式 2：部署成网页链接（分享/售卖用）
和部署教程网页一样：
1. 把 `index.html` 上传到 **Gitee 仓库**（或 GitHub）
2. 开启 **Gitee Pages / GitHub Pages**
3. 生成链接发给别人，点开就能玩，手机上也能玩

---

## 七、常见问题

**Q：双击打开后没有声音？**
A：浏览器规定：必须先**点击一次「开始游戏」**（或点一下屏幕）声音才会开启。开始游戏后点方块就有声音了。也检查一下右上角 ⚙️ 设置里的音量。

**Q：录音按钮点了没反应？**
A：录音需要 https 网页环境（浏览器安全规则）。直接双击文件不支持，部署到网上后就可以用了。上传音频文件不受限制。

**Q：上传的声音没生效？**
A：确认上传后显示了 **✓ 文件名**，且当前音效来源选的是「上传音频」（不是「内置合成」）。点「▶ 试听」可以验证。

**Q：手机上怎么玩？**
A：直接用浏览器打开（或部署链接），手指点方块即可，游戏会自动适配手机屏幕。

**Q：怎么把方块变回默认的彩色渐变？**
A：设置 → 方块外观 → 选「渐变」。

---

*祝玩得开心！🎵 想改玩法、换音色、加功能，都可以随时找 AI 帮忙改这个文件。*
